# Requirements
Intall:
+ Python module: `pip install proxmoxer`
+ kubectl: `curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"`
+ talosctl: `curl -sL https://talos.dev/install | sh`
+ helm: `dnf install helm`
